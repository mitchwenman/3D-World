varying vec3 outNormal;
varying vec3 halfVector;

void main()
{
	gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;

	outNormal = normalise(gl_NormalMatrix * gl_Normal);
	halfVector = normalise(gl_LightSource[0].halfVector.xyz);
}
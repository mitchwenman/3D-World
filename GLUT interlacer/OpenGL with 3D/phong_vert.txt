varying vec3 lightDirection;
varying vec3 normal;
varying vec3 eye;

void main()
{
	gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
	normal = gl_Normal * gl_NormalMatrix;
	eye = -((gl_Vertex * gl_ModelViewMatrix).xyz);
	lightDrection = vec3(gl_LightSource[0].position.xyz - (gl_Vertex * gl_ModelViewMatrix);
}